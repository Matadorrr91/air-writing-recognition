import SwiftUI

/// App-Einstiegspunkt der watchOS-App.
/// (Erstellt Xcode beim Anlegen des Projekts automatisch — hier zur Vollstaendigkeit.)
@main
struct AirWriting_Watch_AppApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
